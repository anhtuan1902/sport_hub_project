export enum ConversationType {
  DIRECT = 'direct',
  COURT_INQUIRY = 'court_inquiry',
  BOOKING = 'booking',
  ANNOUNCEMENT = 'announcement',
}

export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
  LOCATION = 'location',
  SYSTEM = 'system',
}

export enum ParticipantRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  MEMBER = 'member',
}
